# Never copy secrets or local state into an image.
.env
.env.*
!.env.example

.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Tests are run in CI, not shipped in the runtime image.
tests/
pytest.ini

Dockerfile
.dockerignore
